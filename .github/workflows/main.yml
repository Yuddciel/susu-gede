name: BuildJet Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: buildjet-32vcpu-ubuntu-2204

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Setup timezone
        uses: szenius/set-timezone@master
        with:
          timezoneLinux: "Asia/Jakarta"

      - name: Run KSUNEXT Setup
        run: |
          curl -LSs "https://raw.githubusercontent.com/Yuddciel/KernelSU-Next/next-susfs/kernel/setup.sh" | bash -s next-susfs-dev

      - name: Run build KSUNEXT
        run: |
          source ksunext.sh
          echo "ZIPNAME=${ZIPNAME}" >> $GITHUB_ENV

